@namespace Client.Pages.Dashboard.Monitor

<Area Config="_chartConfig" Data="Data" @ref="_chart" TItem="ChartDataItem" />

@code

{

	private IChartComponent _chart;

	private readonly AreaConfig _chartConfig = new() {
		XField = "x",
		YField = "y",
		Height = 84,
		Padding = new[] { 8, 8, 8, 8 },
		ForceFit = true,
		XAxis = new ValueCatTimeAxis {
			Visible = false
		},
		YAxis = new ValueAxis {
			Visible = false
		}
	};

	[Parameter]
	public ChartDataItem[] Data { get; set; }
}