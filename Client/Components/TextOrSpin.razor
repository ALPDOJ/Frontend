@if (Value is null) {
	<Spin />
}
else {
	@(Formatter is null ? Value.ToString() : Formatter(Value))
}

@typeparam T

@code {

	[Parameter]
	public T? Value { get; set; }

	[Parameter]
	public Func<T, string>? Formatter { get; set; }

}